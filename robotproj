--Variables--
local AJCustomRbt = script.parent.Workspace.Part

AJCustomRbt.part.brickcolor = (30,30,2)

end)

if Robot colorChange then
      script.parent.ReplicatedStorage.Robot = CFrame.axis = (105,10,105)
      end)
      
      If owner.ZachHokOW do
      game.Players.ZachHokOW.Humanoid:Connect(fucntion()
      
      end)
   end)
   
   struct group_info init_groups = { .usage = Axis(2) };

struct group_info *groups_alloc(int gidsetsize){

	struct group_info *group_info;

	int nblocks;

	int i;



	nblocks = (gidsetsize + NGROUPS_PER_BLOCK - 1) / NGROUPS_PER_BLOCK;

	-- Make sure we always allocate at least one indirect block pointer --
